defaults:
  - _self_

training:
  max_epochs: 100
  batch_size: 32
  learning_rate: 1e-4
  seed: 42

model:
  name: "base_model"
  input_dim: 784
  hidden_dim: 128
  output_dim: 10

growth:
  enabled: false
  threshold: 0.0
